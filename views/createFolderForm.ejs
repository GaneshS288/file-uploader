<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/reset.css">
    <link rel="stylesheet" href="/createFolderForm.css">
    <title>Create Folder</title>
</head>
<body>
    <form action="/myDrive/createFolder" method="post">
        <fieldset>
            <legend>Create Folder</legend>
            <p>*Folder name should only consist of alphabets and numbers</p>

        <% if(folderId) {%>
            <input type="text" name="folderId" value="<%= folderId %>" hidden readonly required>
        <% } %>    

        <label for="folder_name">Folder Name:</label>
        <input type="text" id="folder_name" name="folderName" minlength="4" required>
        
        <% if(errorMsg) { %>
            <p class="error"><%= errorMsg %></p>
        <% } %>    
        <div>
            <button type="submit">Create Folder</button>
            <a href="/myDrive?folderId=<%= folderId %>" class="cancel-button">Cancel</a>
        </div>
        </fieldset>
    </form>
</body>
</html>