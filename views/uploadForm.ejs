<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/reset.css">
    <link rel="stylesheet" href="/uploadForm.css">
    <script src="/scripts/upload.js" defer></script>
    <title>Upload files</title>
  </head>
  <body>
    <form action="/myDrive/upload" method="post" enctype="multipart/form-data">
      <fieldset>
        <legend>Upload Files</legend>
        <div>
          <p>*Allowed filetypes - .jpg, .png , .jpeg, .txt, .mp4</p>
          <p>
            *File size Limit - Any File can't be larger than 5 MB and you can't
            upload more than 5 files at once
          </p>
        </div>

        <div class="error-container">
          <h2>Files</h2>
        </div>

        <% if(errorMsg) {%>
          <div>
            <p class="server-error"><%= errorMsg %> </p>
          </div>
        <% } %>

        <div class="label-and-button-container">
  
          <% if(folderId) {%>
            <input type="text" name="folderId" value="<%= folderId %>" hidden readonly required>
          <% } %>
  
          <label for="file-input" class="choose-file-label">Choose Files</label>
          <input
            type="file"
            accept=".png, .jpg, .jpeg, .txt, .mp4"
            name="uploaded-files"
            id="file-input"
            multiple
            hidden
            required
          />
  
            <button type="submit">Upload</button>
            <a href="/myDrive?folderId=<%= folderId %>" class="cancel-button">Cancel</a>
        </div>
      </fieldset>
    </form>
  </body>
</html>
