<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/reset.css" />
    <style>
      body {
        display: grid;
        grid-template-columns: minmax(auto, 400px);
        place-content: center;
        min-height: 100vh;
        font-family:
          "Gill Sans", "Gill Sans MT", Calibri, "Trebuchet MS", sans-serif;
        color: var(--primary-light-text-color);
        font-size: 1.2rem;
        padding: 1rem;
      }

      main {
        display: grid;
        row-gap: 2rem;
      }

      main > div {
        display: flex;
      }

      div > p {
        flex: 1;
      }

      div > a {
        padding: 1rem 2rem;
        font: inherit;
        font-weight: 700;
        text-decoration: none;
        color: black;
        border-radius: 5px;
      }

      div:last-child {
        display: flex;
        gap: 1rem;
      }

      div > a:first-child {
        background-color: #1447e6;
        color: whitesmoke;
      }

      div > a:first-child:hover {
        background-color: #1c398e;
      }

      div > a:last-child {
        border: 2px solid black;
      }

      div > a:last-child:hover {
        background-color: gainsboro;
      }
    </style>
    <title>File</title>
  </head>
  <body>
    <main>
      <div>
        <p>Filename</p>
        <p><%= file.name %></p>
      </div>

      <div>
        <p>size</p>
        <p><%= Number.parseInt(file.size / 1024) %>KB</p>
      </div>

      <div>
        <p>Uploaded On</p>
        <p><%= file.created_at.toDateString() %></p>
      </div>

      <div>
        <p>Type</p>
        <p><%= file.type %></p>
      </div>

      <div>
        <a
          href="/myDrive/download/file?fileId=<%= fileId %>&folderId=<%= folderId %>"
          class="download-link"
          >Download</a
        >
        <a href="/myDrive?folderId=<%= folderId %>">Cancel</a>
      </div>
    </main>
  </body>
</html>
